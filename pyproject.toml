[project]
requires-python = ">=3.10"

[project.optional-dependencies]
dev = [
    "black",
    "isort",
    "flake8",
    "pylint",
    "ruff",
]

[tool.pylint.main]
disable = [
    "too-many-branches",
    "too-many-locals",
    "too-many-statements",
]
